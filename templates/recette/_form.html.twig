{# Formulaire principal #}

{{ form_start(form, {'attr': {'enctype': 'multipart/form-data', 'id': 'recette-form'}}) }}

<!-- Champs de base -->
{% include 'recette/partials/_form_basic_fields.html.twig' %}

<!-- Métadonnées -->
{% include 'recette/partials/_form_meta_fields.html.twig' %}

<!-- Ingrédients -->
{% include 'recette/partials/_form_ingredients.html.twig' %}

<hr class="my-4">

<!-- Étapes -->
{% include 'recette/partials/_form_steps.html.twig' %}

<!-- Catégorie -->
<div class="mb-4">
    {{ form_label(form.categorie, 'Catégorie', {'label_attr': {'class': 'form-label fw-bold'}}) }}
    {{ form_widget(form.categorie, {'attr': {'class': 'form-select'}}) }}
    {{ form_errors(form.categorie) }}
</div>

<!-- Boutons d'action -->
<div class="d-flex justify-content-between mt-5">
    <a href="{{ path('app_recette_index') }}" class="btn btn-outline-secondary">
        <i class="bi bi-x-circle"></i> Annuler
    </a>
    <button type="submit" class="btn btn-success px-4">
        <i class="bi bi-check-circle"></i> {{ button_label|default('Enregistrer') }}
    </button>
</div>

{{ form_end(form) }}