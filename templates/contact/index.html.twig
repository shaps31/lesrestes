{% extends 'base.html.twig' %}

{% block title %}Nous contacter - Les Restes{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <!-- Formulaire de contact -->
        <div class="col-lg-8 mx-auto">
            <div class="text-center mb-5">
                <h1 class="text-success mb-3">Nous contacter</h1>
                <p class="text-muted">
                    Une question, une suggestion ou besoin d'aide ? N'hesitez pas a nous ecrire.
                    <br>Nous vous repondrons dans les plus brefs delais.
                </p>
            </div>

            <div class="card border-success shadow-sm">
                <div class="card-body p-4">
                    {{ form_start(form) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.nom, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        <span class="text-danger">*</span>
                        {{ form_widget(form.nom) }}
                        {{ form_errors(form.nom) }}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.email, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        <span class="text-danger">*</span>
                        {{ form_widget(form.email) }}
                        {{ form_errors(form.email) }}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.sujet, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        <span class="text-danger">*</span>
                        {{ form_widget(form.sujet) }}
                        {{ form_errors(form.sujet) }}
                    </div>

                    <div class="mb-4">
                        {{ form_label(form.message, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        <span class="text-danger">*</span>
                        {{ form_widget(form.message) }}
                        {{ form_errors(form.message) }}
                        <small class="form-text text-muted">
                            Minimum 20 caracteres, maximum 2000 caracteres
                        </small>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="bi bi-envelope"></i> Envoyer le message
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

    <!-- Informations de contact -->
    <div class="row mt-5">
        <div class="col-lg-8 mx-auto">
            <div class="row text-center">
                <div class="col-md-4 mb-4">
                    <div class="p-4">
                        <i class="bi bi-envelope-fill text-success fs-1 mb-3"></i>
                        <h5 class="fw-bold">Email</h5>
                        <p class="text-muted">Nous-contacter@Lereste.com</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="p-4">
                        <i class="bi bi-geo-alt-fill text-success fs-1 mb-3"></i>
                        <h5 class="fw-bold">Adresse</h5>
                        <p class="text-muted">666 Rue du Paradis<br>31000 Toulouse</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="p-4">
                        <i class="bi bi-clock-fill text-success fs-1 mb-3"></i>
                        <h5 class="fw-bold">Disponibilite</h5>
                        <p class="text-muted">Lundi - Vendredi<br>9h - 18h</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}